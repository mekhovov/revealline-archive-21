# RevealLine archive 21

Retain the original v0.60.7 and v0.60.8 releases while newer releases become current. This repository contains small original metadata and bounded publication tools. Hosted preparation fetches each published original ZIP in turn, verifies every member against its original manifest, and deletes the temporary ZIP. It never rebuilds a game or commits expanded payload files.

The expected complete site is **1,393 files / 626,433,016 bytes**, leaving **173,566,984 bytes** under the unchanged **800,000,000-byte** cap. Both releases retain their 690 original site members and original release/qualification metadata. All **695 v0.60.7 canonical rows** remain identical; the reviewed outer landing page adds a direct v0.60.8 link. The unchanged Release explorer immediately redirects to the main live catalogue and keeps its visible fallback. An authored inventory is an expectation, not evidence of deployment.

See `source-lock.json` for enforced input pins and `append-v0608-authority.json` for this append's metadata-only preparation scope. `input-authority.json` is preserved byte-identically as the historical initial-v0.60.7 preparation record; its candidate status does not describe current deployment. The predecessor README remains available at commit `2bba1447f2810ec4107eb8b8ddcdf9008c475ad2`.

## Review and publication

Adopt the reviewed append on an isolated `codex/` branch based on the exact existing archive commit `2bba1447f2810ec4107eb8b8ddcdf9008c475ad2`, then inspect a normal PR and merge its exact reviewed head. Recheck the actual base before adoption; a changed base needs a fresh preservation review. This repository and its main-only `github-pages` environment already exist. Do not repeat initial repository seeding or configuration. Exactly one main push starts deployment. Manual dispatch is only for a necessary recorded retry, never a duplicate running deployment. Both workflow jobs remain main-only; no PR deployment or PR CI is advertised.

The workflow checks out pinned source only for the unchanged bounded ZIP extractor and corruption tests. It verifies both original annotated tags, their source trees, metadata, source qualifications, every extracted member and the full final inventory. Preserve contents-read and deploy-only Pages/id-token permissions, non-cancelling concurrency, the 3 GiB runner free-space floor, 800 MB/20,000-file limits and original failure receipts.

Run `python3 -B -m unittest discover -s tools -p 'test_*.py' -v` for small offline fixtures, and the pinned extractor's `test_extract_current.py` cohort separately. These are synthetic checks, not a payload build or public acceptance. Full preparation belongs in the bounded hosted job; this append was prepared locally from small metadata only.

After successful hosted deployment, retain actual archive source/tree/run/deployment/status and original receipt descriptors. Audit every one of the 1,393 expected public files with fresh before/after authority checks. Prove that all 695 predecessor version rows remain byte-identical. Separately exercise each retained version's title, actual flight, Pause/Resume and Release explorer → current main catalogue → Back. Check the newly added landing-page link. Only then may the main publisher admit this expanded archive, append the expanded acceptance evidence and advance its admission record while preserving earlier originals. Existing v0.60.7 acceptance does not qualify this append. Archive-origin storage does not migrate main-origin saves. No offline, physical-device, BFCache or whole P03/P05/P18 acceptance follows from these checks.

Future appends must preserve canonical rows and fit the same cap. Never overwrite original release assets, reuse stale admission evidence, repurpose existing URLs or raise the cap.
